#include "utils.hpp"

int square(int n)
{
    return n * n;
}

int main()
{
    read_lines();
    //read_lines(10);
    int *a = nullptr;
    int res = square(2);

    /*
    for(size_t i = 10; i >= 0; --i)
    {
    }*/
    return *a;
}
